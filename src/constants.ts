// Shared constants — single source of truth so the settings UI default and the
// runtime default never drift.
export const DEFAULT_FOLDER = "Time Log/Sessions";
export const BASE_FILENAME = "Time per project.base";
